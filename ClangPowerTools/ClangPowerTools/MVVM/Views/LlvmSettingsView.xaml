<UserControl
  x:Class="ClangPowerTools.Views.LlvmSettingsView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:ClangPowerTools"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  d:DataContext="{d:DesignInstance local:LlvmSettingsViewModel,
                                   IsDesignTimeCreatable=True}"
  d:DesignHeight="350"
  d:DesignWidth="770"
  mc:Ignorable="d">

  <Grid>
    <Grid.Background>
      <ImageBrush ImageSource="/ClangPowerTools;component/Resources/SettingsBackground.png" Stretch="UniformToFill" />
    </Grid.Background>
    <Grid.RowDefinitions>
      <RowDefinition Height="5" />
      <RowDefinition Height="*" />
      <RowDefinition Height="5" />
    </Grid.RowDefinitions>

    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>


    <!--  Checks list  -->
    <ListView
      Grid.Row="1"
      Grid.Column="0"
      Background="Transparent"
      Focusable="False"
      ItemsSource="{Binding Llvms}"
      ScrollViewer.CanContentScroll="True"
      SelectedItem="{Binding SelectedLlvm}">

      <ListView.ItemContainerStyle>
        <Style TargetType="{x:Type ListViewItem}">
          <Setter Property="Background" Value="Transparent" />
          <Setter Property="Template">
            <Setter.Value>
              <ControlTemplate TargetType="{x:Type ListViewItem}">
                <ContentPresenter />
              </ControlTemplate>
            </Setter.Value>
          </Setter>
        </Style>
      </ListView.ItemContainerStyle>

      <ListView.ItemTemplate>
        <DataTemplate>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="20" />
              <ColumnDefinition Width="400" />
              <ColumnDefinition Width="150" />
              <ColumnDefinition Width="100" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
              <RowDefinition Height="25" />
            </Grid.RowDefinitions>
            <TextBlock
              Grid.Column="1"
              Padding="0,0,200,0"
              Text="{Binding Name}" />
            <ProgressBar
              Grid.Column="2"
              Width="100"
              Height="20"
              Foreground="Green"
              Maximum="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext.SelectedLlvm.MaxProgress}"
              Minimum="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext.SelectedLlvm.MinProgress}"
              Value="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext.SelectedLlvm.DownloadProgress}" />
            <Button
              Grid.Column="3"
              Width="40"
              Height="30"
              Command="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext.DownloadCommand}"
              Content="D"
              FontSize="16" />
            <CheckBox
              Grid.Column="4"
              HorizontalAlignment="Right"
              IsChecked="{Binding IsSelected}" />
          </Grid>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>

    <Button
      Grid.Row="2"
      Grid.Column="0"
      Width="180"
      Height="40"
      Command="{Binding DeleteCommand}"
      Content="Delete"
      FontSize="16"
      Visibility="Hidden" />


    <!--  Download LLVM
    <Button
      Grid.Row="1"
      Grid.Column="0"
      Width="180"
      Height="40"
      Command="{Binding DownloadCommand}"
      Content="Download"
      FontSize="16" />


    <Button
      Grid.Row="2"
      Grid.Column="0"
      Width="180"
      Height="40"
      Command="{Binding DeleteCommand}"
      Content="Delete"
      FontSize="16" />


    <Button
      Grid.Row="3"
      Grid.Column="0"
      Width="180"
      Height="40"
      Command="{Binding StopCommand}"
      Content="Stop"
      FontSize="16" />

    <ProgressBar
      Grid.Row="4"
      Grid.Column="0"
      Width="100"
      Height="20"
      Foreground="Green"
      Maximum="{Binding LlvmModel.MaxProgress}"
      Minimum="{Binding LlvmModel.MinProgress}"
      Value="{Binding LlvmModel.DownloadProgress}" />-->
  </Grid>
</UserControl>

